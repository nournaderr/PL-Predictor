[project]
name = "pl-predictor"
version = "0.1.0"
description = "Premier League match outcome classifier"
authors = [
    {name = "nournaderr",email = "nournader118@gmail.com"}
]
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
